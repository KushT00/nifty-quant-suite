# Core Scientific Stack
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0

# Live Trading SDK (Option Buying & Selling strategies)
# openalgo>=1.0.0

# No external pricing libraries — Black-Scholes is implemented from scratch
# No ta-lib — ADX, SMA, EMA, RMA, Supertrend all implemented natively
